import styled from 'styled-components';

export const InterviewRoomFooter = styled.footer`
  position: fixed;
  left: 0;
  bottom: 0;

  width: 100%;

  padding: 1rem 0;

  display: flex;
  justify-content: center;
  justify-self: flex-end;
`;

export const StartTimeWrap = styled.span`
  position: absolute;
  left: 2rem;
`;
