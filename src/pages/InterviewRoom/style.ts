import styled from 'styled-components';
import { colors } from '@src/styles/variables';

export const InterviewRoom = styled.main`
  background: ${colors.darkGray};
  width: 100vw;
  height: 100vh;
`;
